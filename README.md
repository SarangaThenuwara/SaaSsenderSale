# SaaS Sender — Premium (Tailwind UI)

Prerequisites
- Node.js (for Tailwind build)
- Python 3.11+, pip, virtualenv
- MongoDB and Redis (or use Docker)
- Backblaze B2 S3 credentials
- Supabase project (SUPABASE_URL & SUPABASE_ANON_KEY)
- FERNET_KEY and SECRET_KEY env vars

Install Python deps: